#!/usr/bin/node
// print strings from array with loop
const myStrings = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let i = 0;

while (myStrings[i]) {
  console.log(myStrings[i]);
  i++;
}
